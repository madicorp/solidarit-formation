import ReactDOM from 'react-dom';
import React from 'react';
import FormationAdmin from './components/formations-admin';

ReactDOM.render(
	<FormationAdmin />,
	document.getElementById( 'formations-admin' )
);
